<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Gerenciador de Clientes</title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= action_cable_meta_tag %>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="A Client Manager for Law purposes.">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "turbo", type: "module" %>

    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#61b9d2">
    <meta name="msapplication-TileColor" content="#da532c">
    <meta name="theme-color" content="#ffffff">
  </head>

  <body class="flex flex-col h-screen justify-between">
    <div class="bg-gray-700">
      <div class="max-w-screen-xl mx-auto py-3 px-2 sm:px-6 lg:px-1">
        <div class="flex items-left justify-between flex-wrap">
          <div class="mt-2 order-3 w-full sm:mt-0 sm:w-auto">
            <div class="rounded-md shadow-sm flex space-x-2">
              <%= link_to "Home", root_path, class: "bg-black text-white py-2 px-4 rounded-md font-semibold" %>
              <%= link_to "Pessoas", people_path, class: "bg-black text-white py-2 px-4 rounded-md font-semibold" %>
              <%= link_to "Clientes", clients_path, class: "bg-black text-white py-2 px-4 rounded-md font-semibold" %>
              <%= link_to "Partes Contrárias", opposing_parties_path, class: "bg-black text-white py-2 px-4 rounded-md font-semibold" %>
            </div>
          </div>
        </div>
      </div>
    </div>

    <main class="mx-1 mb-auto">
      <% flash.each do |key, message| %>
        <div class="alert <%= flash_class(key) %>">
          <%= message %>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <footer class="text-center mt-6 pb-6 h-10">
      <p class="text-gray-500">
        Coded by
        <a class="border-b mb-1" href="mailto:johny.carebe@gmail.com">Johny Carebe</a>
      </p>
    </footer>
  </body>
</html>
